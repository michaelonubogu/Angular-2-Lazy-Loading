/* ----------------------------------------------------------------------
 * Name: Authentication Component
 * Description: Handles login, forgot/2-factor authentication for the app
 * --------------------------------------------------------------------*/
import { Component } from '@angular/core';
import { Pipe, PipeTransform } from '@angular/core';

@Component({
  selector: 'ovs-login',
  templateUrl: 'login.html'
})

export class LoginWidget {

  constructor() {
  }
}

